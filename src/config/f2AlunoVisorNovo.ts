/**
 * FLAG do visor novo do aluno F2 (redesenho das abas: Missoes, Casa, Chat),
 * seguindo o mockup aprovado pelo Fundador. A aba Inicio usa F2_ALUNO_HOME_NOVA.
 *
 * DEFAULT FALSE: desligada, cada aba renderiza EXATAMENTE como hoje, sem
 * regressao. So o Fundador liga (localhost primeiro; nunca commitar true).
 * Rascunho: vira producao so depois de Design + Conteudo + ok do Fundador.
 */
export const F2_ALUNO_VISOR_NOVO = true; // LIVE em producao desde o lancamento do visor novo (ordem do Fundador 13/07). Reversivel: false + deploy.

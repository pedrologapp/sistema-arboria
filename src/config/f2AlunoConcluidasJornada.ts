/**
 * FLAG da aba Concluidas como JORNADA (nao mais lista plana): missoes concluidas
 * agrupadas pelas fases percorridas, com contador pessoal no topo e a data de
 * cada conclusao. E a 1a parede do "dar o eu ao aluno" (Passo 2 do roadmap).
 * Progresso PESSOAL e PRIVADO, jamais ranking ou score comparavel.
 *
 * DEFAULT FALSE: desligado, a aba Concluidas fica EXATAMENTE como hoje (lista
 * plana + chips de filtro). So o Fundador liga (localhost; nunca commitar true).
 */
export const F2_ALUNO_CONCLUIDAS_JORNADA = true; // LIVE em producao (ordem do Fundador 13/07). Reversivel: false + deploy.

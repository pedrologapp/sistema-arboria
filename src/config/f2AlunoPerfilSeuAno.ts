/**
 * FLAG do "Seu ano" no Perfil do aluno F2: a linha do tempo pessoal (missoes
 * concluidas + entrada em capitulos). Vive DENTRO do visor novo.
 * Independente da Arvore de Talentos (F2_ALUNO_PERFIL_ARVORE), que o Fundador
 * pediu pra deixar desligada por enquanto. Progresso do proprio aluno, privado,
 * aditivo, sem ranking nem comparacao.
 *
 * DEFAULT FALSE. So o Fundador liga (localhost primeiro; nunca commitar true).
 */
export const F2_ALUNO_PERFIL_SEU_ANO = true; // LIVE em producao (ordem do Fundador 13/07). Reversivel: false + deploy.
